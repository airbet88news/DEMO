<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AB88Stream - Cyber Grid Live HD</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;800&display=swap" rel="stylesheet">
    <style>
        /* ---------------------------------------------------- */
        /* 1. VARIABEL TEMA */
        /* ---------------------------------------------------- */
        :root {
            /* Default Dark Mode */
            --bg-color: #1a1a2e;
            --container-bg: rgba(0, 0, 0, 0.7);
            --text-color: #f0f0f0;
            --accent-red: #ff3366; 
            --accent-yellow: #00e5ff; 
            --shadow-3d: rgba(0, 0, 0, 0.9);
            --grid-dot-color: rgba(255, 255, 255, 0.05); 
            --box-glow: 0 0 20px rgba(0, 229, 255, 0.5);
            --ticker-bg: #ff3366; /* Merah Ticker */
            --ticker-text: white;
            --transition-speed: 0.5s;
        }

        /* Light Mode */
        body.light-mode {
            --bg-color: #f0f0f0;
            --container-bg: rgba(255, 255, 255, 0.85);
            --text-color: #1a1a1a;
            --accent-red: #cc0000;
            --accent-yellow: #ffaa00;
            --shadow-3d: rgba(0, 0, 0, 0.2);
            --grid-dot-color: rgba(0, 0, 0, 0.05);
            --box-glow: 0 0 20px rgba(255, 170, 0, 0.5);
            --ticker-bg: #cc0000; /* Merah Ticker */
            --ticker-text: white;
        }

        /* ---------------------------------------------------- */
        /* 2. BACKGROUND CYBER GRID UNIK */
        /* ---------------------------------------------------- */
        body {
            font-family: 'Montserrat', sans-serif;
            margin: 0;
            background-color: var(--bg-color);
            color: var(--text-color);
            display: flex;
            flex-direction: column;
            align-items: center;
            min-height: 100vh;
            padding: 0;
            transition: background-color var(--transition-speed);
            position: relative;
        }
        
        #cyber-grid-bg {
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100%; z-index: -2;
            background-image: radial-gradient(var(--grid-dot-color) 1px, transparent 1px);
            background-size: 40px 40px;
            animation: moveGrid 30s linear infinite;
            transition: background-image var(--transition-speed);
        }
        
        @keyframes moveGrid {
            from { background-position: 0 0; }
            to { background-position: 40px 40px; }
        }

        /* Tombol Toggle Mode */
        #mode-toggle {
            position: fixed;
            top: 70px; right: 20px; padding: 8px 15px;
            background-color: var(--container-bg); 
            color: var(--text-color); 
            border: 1px solid var(--text-color);
            border-radius: 5px;
            cursor: pointer;
            z-index: 2000;
            transition: all var(--transition-speed);
        }
        
        /* ---------------------------------------------------- */
        /* 3. LAYOUT & STYLING UTAMA */
        /* ---------------------------------------------------- */
        .navbar {
            width: 100%; background-color: rgba(0, 0, 0, 0.7); backdrop-filter: blur(8px);
            box-shadow: 0 4px 20px var(--shadow-3d); padding: 0 40px; display: flex;
            align-items: center; justify-content: space-between; position: sticky;
            top: 0; z-index: 1000;
            transition: background-color var(--transition-speed), box-shadow var(--transition-speed);
        }

        .live-24-box {
            background-color: var(--accent-red); color: white; font-weight: 800;
            padding: 20px 20px; font-size: 1.1em; margin-right: 30px;
            box-shadow: 5px 0 15px rgba(255, 51, 102, 0.6);
            transition: background-color var(--transition-speed), box-shadow var(--transition-speed);
            display: flex; align-items: center; gap: 10px;
        }

        .nav-link-group { flex-grow: 1; display: flex; justify-content: flex-start; }
        .nav-link { color: var(--text-color); padding: 20px 25px; font-weight: 600; text-decoration: none; position: relative; }
        .nav-link.active::after { content: ''; position: absolute; bottom: 0; left: 0; width: 100%; height: 3px; background-color: var(--accent-red); box-shadow: 0 0 10px var(--accent-red); }
        .nav-link:hover, .nav-link.active { color: var(--accent-red); }
        
        .main-container {
            width: 90%; max-width: 1200px; margin-top: 50px; padding: 30px;
            background-color: var(--container-bg); backdrop-filter: blur(15px);
            border-radius: 15px; border: 2px solid rgba(255, 255, 255, 0.1);
            box-shadow: 0 15px 50px var(--shadow-3d), var(--box-glow);
            position: relative; z-index: 10;
            transition: background-color var(--transition-speed), box-shadow var(--transition-speed);
            margin-bottom: 30px; /* Tambahkan margin bawah */
        }
        
        /* Video Wrapper */
        .video-player-wrapper {
            position: relative; width: 100%; padding-top: 56.25%; height: 0; background-color: #000;
            margin-bottom: 30px; border-radius: 10px; overflow: hidden;
            box-shadow: 0 0 25px rgba(255, 255, 255, 0.1), 0 0 10px var(--accent-red);
        }
        
        /* ---------------------------------------------------- */
        /* 4. NEWS TICKER BAWAH (KOTAK MERAH) */
        /* ---------------------------------------------------- */
        .bottom-ticker-wrapper {
            width: 100%;
            overflow: hidden;
            background-color: var(--ticker-bg);
            color: var(--ticker-text);
            padding: 10px 0;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
        }
        
        .bottom-ticker {
            display: inline-block;
            white-space: nowrap;
            padding-left: 100%; 
            animation: ticker-move 70s linear infinite; /* Kecepatan lebih cepat */
        }

        .ticker-text-item {
            font-size: 1.1em;
            font-weight: 600;
            margin-right: 80px;
            letter-spacing: 0.5px;
        }

        @keyframes ticker-move {
            0% { transform: translateX(0%); }
            100% { transform: translateX(-100%); }
        }

        /* ---------------------------------------------------- */
        /* 5. FOOTER LOGO BAR (Di bawah Ticker) */
        /* ---------------------------------------------------- */
        .logo-footer-bar {
            width: 100%;
            padding: 20px 0;
            text-align: center;
            background-color: rgba(0, 0, 0, 0.8);
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            box-shadow: 0 -5px 15px rgba(0, 0, 0, 0.5);
        }
        
        .logo-footer-bar h3 {
            font-size: 0.9em;
            color: #888;
            margin-bottom: 15px;
        }
        
        .logo-slot {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 40px;
        }
        
        .logo-slot img {
            height: 30px; /* Ukuran standar logo */
            opacity: 0.7;
            transition: opacity 0.3s;
        }
        .logo-slot img:hover {
            opacity: 1;
        }
        
        /* Sisa Styling Dasar */
        .video-player-wrapper iframe { position: absolute; top: 0; left: 0; width: 100%; height: 100%; border: none; }
        .stream-info-box { border-left: 5px solid var(--accent-red); padding-left: 20px; }
        .on-air-label { display: inline-block; background-color: var(--accent-red); color: white; padding: 5px 10px; font-weight: 800; font-size: 0.8em; margin-bottom: 5px; border-radius: 3px; text-transform: uppercase; }
        .stream-title { font-size: 1.8em; font-weight: 700; margin-bottom: 5px; color: var(--text-color); }
        .stream-desc { font-size: 1em; line-height: 1.5; color: var(--secondary-text, #b0b0b0); }
    </style>
</head>
<body>
    
    <div id="cyber-grid-bg"></div>
    
    <button id="mode-toggle">Light Mode ☀️</button>

    <nav class="navbar">
        <div class="live-24-box">
            <span style="font-size: 0.9em;">LIVE</span>
            <span id="current-time" class="live-time">00:00:00</span>
        </div>
        
        <div class="nav-link-group">
            <a href="#" class="nav-link active">BREAKING NEWS</a>
        </div>
    </nav>

    <div class="main-container">
        
        <div class="video-header-title">LIVE STREAMING AIRBET88 24 JAM</div>

        <div class="video-player-wrapper">
            <iframe 
                src="https://www.youtube.com/embed/T5XD4ocLuXg?si=GoppSkn5u0ke0lCm" 
                title="Live Stream HD" 
                allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" 
                allowfullscreen>
            </iframe>
        </div>

        <div class="stream-info-box">
            <div class="on-air-label">ON AIR SEKARANG</div>
            <h1 class="stream-title">AIRBET88 TV : INDEPENDEN & TERPERCAYA</h1>
            <p class="stream-desc">Layanan resmi live streaming AIRBET88 TV dengan kualitas HD. Menampilkan berita, analisis, dan liputan eksklusif 24/7. Tonton melalui perangkat apa pun, tanpa batas waktu.</p>
        </div>
    </div>
    
    <div class="bottom-ticker-wrapper">
        <div class="bottom-ticker" id="bottom-ticker-content">
            <span class="ticker-text-item">🚨 HEADLINE: Pasar saham global merespons kebijakan baru dengan volatilitas tinggi.</span>
            <span class="ticker-text-item">✅ EKSPERTI ANALISIS: Peluang dan risiko investasi di sektor teknologi.</span>
            <span class="ticker-text-item">🌍 DUNIA: Konflik diplomatik di kawasan timur mencapai puncaknya.</span>
        </div>
    </div>

    <div class="logo-footer-bar">
        <h3>Mitra Resmi Kami</h3>
        <div class="logo-slot">
            <img id="logo-brand-2" src="https://via.placeholder.com/100x30/555/FFF?text=BRAND+B" alt="Airbet88">
            </div>
    </div>


    <script>
        // =========================================================
        // 1. LOGIC JAM OTOMATIS
        // =========================================================
        function updateClock() {
            const now = new Date();
            const hours = String(now.getHours()).padStart(2, '0');
            const minutes = String(now.getMinutes()).padStart(2, '0');
            const seconds = String(now.getSeconds()).padStart(2, '0');
            
            document.getElementById('current-time').textContent = `${hours}:${minutes}:${seconds}`; 
            
            // Perbarui setiap 1 detik
            setTimeout(updateClock, 1000); 
        }
        updateClock();


        // =========================================================
        // 2. LOGIC DARK/LIGHT MODE
        // =========================================================
        const body = document.body;
        const toggleButton = document.getElementById('mode-toggle');
        
        // Atur status awal ke Dark Mode
        body.classList.remove('light-mode');
        toggleButton.textContent = 'Light Mode ☀️';

        toggleButton.addEventListener('click', () => {
            body.classList.toggle('light-mode');
            
            if (body.classList.contains('light-mode')) {
                toggleButton.textContent = 'Dark Mode 🌙';
            } else {
                toggleButton.textContent = 'Light Mode ☀️';
            }
        });
        
        // =========================================================
        // 3. PENGATURAN TEKS DAN GAMBAR LOGO
        // =========================================================
        const editableContent = {
'ticker-content':
    '<span class="ticker-text-item">🚨 HEADLINE: Pasar saham global merespons kebijakan baru dengan volatilitas tinggi.</span>' +
    '<span class="ticker-text-item">✅ EKSPERTI ANALISIS: Peluang dan risiko investasi di sektor teknologi.</span>' +
    '<span class="ticker-text-item">🌍 DUNIA: Konfllik diplomatik di kawasan timur mencapai puncaknya.</span>' +
    '<span class="ticker-text-item">♠️ POKER: Turnamen mingguan dimulai malam ini — hadiah utama meningkat 25%!</span>' +
    '<span class="ticker-text-item">⚽ BOLA: Prediksi pertandingan besar — strategi taruhan & line-up terbaru.</span>' +
    '<span class="ticker-text-item">🎰 SLOT: Jackpot progresif meningkat — putaran beruntun tercatat hari ini.</span>' +
    '<span class="ticker-text-item">📰 LIPUTAN: Wawancara eksklusif dengan legenda poker regional — baca insightnya.</span>' +
    '<span class="ticker-text-item">📢 PROMO: Klaim bonus deposit 50% untuk game slot tertentu — hanya 24 jam.</span>' +
    '<span class="ticker-text-item">⚽ HIGHLIGHT: Gol spektakuler menit akhir — tinjauan ulang momen terbaik.</span>' +
    '<span class="ticker-text-item">♣️ STRATEGI POKER: Cara membaca lawan di meja — tips dari pemain pro.</span>' +
    '<span class="ticker-text-item">🎯 SLOT TIPS: Pilih volatilitas yang cocok — panduan cepat untuk pemula.</span>' +
    '<span class="ticker-text-item">📊 STATISTIK: Persentase kemenangan permainan bola virtual minggu ini.</span>' +
    '<span class="ticker-text-item">🔥 HOT: Sesi live poker cash game sekarang — join meja & raih hadiah.</span>' +
    '<span class="ticker-text-item">🎁 GIVEAWAY: Free spin untuk pemain baru — cek syarat dan klaim sekarang.</span>' +
    '<span class="ticker-text-item">⚠️ PENGUMUMAN: Maintenance server terjadwal — beberapa game mungkin tidak tersedia.</span>' +
    '<span class="ticker-text-item">🏆 LEADERBOARD: Siapa top player slot & poker minggu ini? Cek ranking terbaru.</span>',
            // Ganti URL logo di sini
            'logo-brand-1': 'https://ibb.co/B2BHgyc9',
            'logo-brand-2': 'https://ibb.co/B2BHgyc9',
            'logo-brand-3': 'https://ibb.co/B2BHgyc9',
        };
        
        document.getElementById('bottom-ticker-content').innerHTML = editableContent['ticker-content'];
        document.getElementById('logo-brand-1').src = editableContent['logo-brand-1'];
        document.getElementById('logo-brand-2').src = editableContent['logo-brand-2'];
        document.getElementById('logo-brand-3').src = editableContent['logo-brand-3'];
        
    </script>
</body>
</html>
